import { styles } from './styles';
import { View } from 'react-native';

interface DivisorProps { }

export const Divisor = ({ }: DivisorProps) => {
    return (
        <View style={styles.divisor}>
        </View>
    )

}
